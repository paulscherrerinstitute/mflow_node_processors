package:
    name: mflow_node_processors
    version: 0.0.1

build:
  number: 0

source:
    path: ..

requirements:
    build:
        - python
    run:
        - python
        - mflow >=0.0.16
        - bitshuffle ==0.2.4
        - bottle
        - h5py >=2.7.*
        - hdf5 ==1.8.17
        - numpy
        - requests

test:
  source_files:
    - tests/

about:
    summary: "Mflow node processors."
