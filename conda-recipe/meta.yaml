package:
    name: mflow_node_processors
    version: 1.2.7

build:
  number: 0

source:
    path: ..

requirements:
    build:
        - python
    run:
        - python
        - numpy
        - hdf5 ==1.8.17
        - h5py >=2.7.*
        - bitshuffle ==0.2.4
        - mflow_nodes >=0.0.5
        - bsread

test:
  source_files:
    - tests/

about:
    summary: "mflow node processors"
